defaults:
  - _self_
  - /encoder@experiment.encoder: vmamba
  - /loss: ce
  - /datamodule: default
  - /trainer: default
  - /logger: wandb
  - /callbacks: default

model:
  _target_: src.experiment.vmamba.VmambaModule
  encoder: '${experiment.experiment.encoder}' 
  loss: ${experiment.loss}
  max_epochs: ${experiment.trainer.max_epochs}

name: vmamba

